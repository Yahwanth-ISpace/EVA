import * as IconsTypes from "./iconsTypes";
import * as AuthTypes from "./authTypes";
import * as AppointmentTypes from "./appointmentsTypes";
import * as CoverageTypes from "./coverageTypes";
import * as PayeeTypes from "./payeeTypes";
import * as PatientTypes from "./patientTypes";
import * as ProviderTypes from "./providerTypes";
import * as OfficeTypes from "./officeTypes";
import * as ChatsTypes from "./chatsTypes";
import * as InsuranceTypes from "./insuranceTypes";
import * as VerificationTypes from "./verificationTypes";

const apptypes = {
  iconTypes: IconsTypes,
  auth: AuthTypes,
  appointments: AppointmentTypes,
  coverage: CoverageTypes,
  payees: PayeeTypes,
  patients: PatientTypes,
  providers: ProviderTypes,
  offices: OfficeTypes,
  chats: ChatsTypes,
  insurances: InsuranceTypes,
  verifications: VerificationTypes,
};

export default apptypes;
